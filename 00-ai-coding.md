# AI Coding（总览与选型）

本篇是背景与选型说明。**想直接动手，请从 [README](README.md) 开始。**

---

# 1 为什么以命令行工具为主线

比较模型和工具时，可以从**两条主线**看：开放程度，以及模型服务的来源。

**第一条：开源 vs 闭源。** 这里实际是两个层面，别混在一起：**模型**的开放权重/闭源，和**客户端**的开源/闭源。模型层面，GPT、Claude、Gemini 等是闭源模型；DeepSeek、GLM（智谱）、Qwen（阿里）、MiMo 等提供开放权重模型。客户端层面，我们常用的工具里多数已经是开源的（连 Codex 的命令行客户端也是开源的，只是它默认接的 GPT 模型闭源）。两层可以分别选择，不必把客户端和模型绑在一起。

**第二条：中 vs 美。** 模型供给来自不同厂商和地区：同一个任务，从哪边拿额度、多少钱、能否持续使用，需要分别评估。[统一接入](02-unified-access.md) 第 4 节列的是**当前中转可用的模型**，没有列出的模型不代表能力弱，只代表这套配置暂时没有接入。

这两条线落到我们身上只有一句话：**不要把工作流绑死在单一厂商的单一模型上**——后面整套文档（统一接入 + 多工具 + 环境变量管密钥）就是为这句话服务的。

格局说完，回到工具本身。在 AI 辅助编程赛道中，率先出圈的是 **Cursor**——它在 VS Code 编辑器基础上深度集成 AI 编程能力，至今仍是 IDE 流派的代表工具。

而在**命令行流派**中，**Anthropic 的 Claude Code** 率先走红，一度是该领域的事实标准。OpenAI 后来推出的 **Codex** 同样聚焦命令行场景，能力上与 Claude Code 属于同一梯队——**Codex 并没有在能力上超过 Claude Code**（推理细致度上 Claude Code 反而常被认为更强）。它之所以是我们的主力，靠的是下面几条，而不是"更聪明"：工具调用稳、三形态齐全、客户端开源。

命令行类的编程智能体，因交互方式简洁、扩展性强，往往是最新功能的**首发阵地**——新模型、新工具调用能力、新的"智能体协议"通常先在 CLI 里落地，再慢慢进到 IDE 里。因此，我们**以命令行作为学习与配置的主线**：配一次，各形态通用。

但"主线是命令行"不等于"只能开终端"。**Codex 的桌面形态现在已经很好用**——它位于 **ChatGPT 桌面应用**中。不想开终端时可以使用桌面端的 Codex；它与 CLI 共用 `~/.codex/config.toml` 和项目中的 `AGENTS.md`。首次启动仍需按应用提示完成认证，见 [Codex](03-codex.md) 第 3.3 节。

所以我们的态度是：**命令行是主线，桌面版 / IDE 扩展按场景取用。** 选工具的顺位也随之调整：

| 顺位 | 工具 | 为什么排在这里 |
| --- | --- | --- |
| 1 | **Codex** | 与 GPT 系列同源，工具调用最稳；终端 / VS Code 扩展 / ChatGPT 桌面应用三形态齐全，**命令行客户端开源**；是目前的首选（不是因为比 Claude Code 更聪明，而是更好用、更好控） |
| 2 | **pi** | 开源、轻量、可扩展；接国产模型 + 长上下文最合适 |
| 3 | **OpenCode** | 开源通用框架，模型接入灵活；OpenCode Console 还提供限时免费模型，接入方法见 [OpenCode](05-opencode.md) 第 4.1 节 |
| 4 | **Claude Code** | 推理细致、生态成熟；接第三方模型时需额外验证兼容性，选型注意事项见下 |

**关于 Claude Code**：它在推理细致度上依然有优势，但使用时要分别检查两类问题：

1. **事实性回答**：先确认客户端当前连接的是哪个模型，再用可靠资料核对关键结论。若同一模型持续给出不可靠的回答，换模型后再比较；只换客户端未必有效。
2. **第三方模型兼容性**：未知模型可能触发 `[claude-code:unrecognized_model]` 警告；这涉及客户端对上下文窗口等参数的假设，不足以证明故意限制。图片和长会话的问题需要按版本、模型、传入方式分别复现，见 [Claude Code](06-claude-code.md) 第 8 节。

这里还有两个现实原因。一是**客户端的开放程度不同**：Claude Code 客户端闭源，Codex 的命令行客户端开源（可审计、可改）；第三方模型在任一客户端上都应实际验证工具调用与图片处理。另一方面，两家的**模型额度**都握在公司手里，这也是我们不只靠一家模型的原因。

二是**成本与可控性**：闭源旗舰按 token 计费、想用就得持续掏钱，而开源工具（pi、OpenCode，以及开源的 Codex CLI）能直接接国产开放权重模型（Kimi、DeepSeek、GLM、Qwen 等）——便宜、可换；即使各家的订阅都已开通，也仍然覆盖不到所有使用场景，所以我们需要开源这条腿。

---

# 2 我们现在用哪几个工具


| 优先级 | 工具              | 形态                     | 一句话定位                         |
| --- | --------------- | ---------------------- | ----------------------------- |
| ★★★ | **Codex**       | 终端 + VS Code 扩展 + ChatGPT 桌面应用 | 主力：与 GPT 系列同源，工具调用最稳；命令行客户端开源 |
| ★★  | **pi**          | 终端                     | 次主力：开源、轻量、可扩展，接国产模型 + 长上下文最合适 |
| ★   | **OpenCode**    | 终端（另有桌面/IDE 入口）        | 第三顺位：接入灵活，可选用 Console 的限时免费模型 |
| ★   | **Claude Code** | 终端 + VS Code 扩展        | 末位：推理细致、生态成熟；第三方模型兼容性需单独验证 |
| ★★  | **Orca**        | 桌面应用 + CLI（**编排层**）     | 进阶：开源"智能体工作台"（ADE），一个任务一个 worktree，多个 CLI 智能体并行；**不接模型、不存密钥** |


**为什么是这四个**：客户端层面，Codex 命令行、pi、OpenCode 都是开源的，只有 Claude Code 闭源；模型层面，可以按任务在闭源模型（GPT / Claude 等）与开放权重模型（DeepSeek、GLM、Qwen 等）之间选择。**顺位按第 1 节的表来**：Codex → pi → OpenCode → Claude Code。

**Orca 不是第五个"模型客户端"**：它自己不接模型、不含密钥，而是把你已经配好的上面几个 CLI 智能体放进同一个窗口里调度——每个任务一个 git worktree，互不踩文件，还可以把同一个需求同时丢给多个智能体比拼结果。所以它排在上面四个之后：**先会用智能体，再来谈编排。**

## 2.1 关于 OpenCode

OpenCode 是一个受 Claude Code 启发而创建的开源 AI 编程智能体，社区活跃、更新极快，已经发展成为一个能真正干活的独立产品（**v2** 版本）。

它的短板也很清楚：**它本身不产出模型**，能力上限取决于你给它接哪个模型。所以同样的任务，它的结果通常不如 Codex、pi 稳定。但它是可用的，而且开源、可改、可审计，值得动手试一试。

---

# 3 编程智能体正在变成通用智能体

编程智能体的能力边界早已超越代码本身。从文献检索到论文撰写，从方案设计到实验执行，完整的学术研究流程正在被系统性"智能化"：

- Claude Code 上的开源工具包 `academic-research-skills`，覆盖研究、写作、审稿和发表四个环节；
- `scholar-skill` 插件提供了覆盖 26 项科研技能的完整研究管线——读论文、找论文、讨论创新点、实现创新点、修改代码、跑实验，都可以交由智能体辅助完成。

我们所要做的事情，就是**学习好这些智能体的使用方法，理解它们各自的能力边界**，进而将其应用于科研实践——用最好的工具，做出更好的科研成果。

---

# 4 文档索引

完整索引（含“什么时候看”）只在 [README](README.md) 第 3 节维护一份，这里不再重复。按编号速查：

| 编号 | 文档 | 主题 |
| --- | --- | --- |
| 01 | [01-prerequisites.md](01-prerequisites.md) | 前置工具（Node.js / Git for Windows / Windows Terminal + PowerShell 7 / VS Code） |
| 02 | [02-unified-access.md](02-unified-access.md) | 统一接入：密钥、地址与协议、可用模型清单 |
| 03–06 | [03-codex.md](03-codex.md)｜[04-pi.md](04-pi.md)｜[05-opencode.md](05-opencode.md)｜[06-claude-code.md](06-claude-code.md) | 四个 CLI 工具完整教程 |
| 07–09 | [07-orca.md](07-orca.md)｜[08-cc-switch.md](08-cc-switch.md)｜[09-matt-skills.md](09-matt-skills.md) | 进阶：编排层、多供应商、技能包 |
| 10 | [10-appendix-full-config.md](10-appendix-full-config.md) | 附录：我们机器上的完整配置要点 |
